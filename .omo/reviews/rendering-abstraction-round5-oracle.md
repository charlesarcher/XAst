# Round 5 — Independent Oracle Receipt (rendering-abstraction)

- **Lane:** independent (oracle) · **Round:** 5 · **Launch ID:** launch-r5-oracle-01
- **Session:** ses_fd690e861ffeRvXwLA2yyRx8ZN (bg_bb0e473a)
- **Target:** .omo/plans/rendering-abstraction.md · **SHA-256:** fcdd54e3420a83530984abf6bbdf55c8743f145f3d7cdfd9a6a122f15baa3be2 (recomputed pre-review; matched; shape conformed: TL;DR L9–11, 48 contiguous rows, full 1045 lines read)
- **Workspace root:** /home/archerc/code/XAst · **Runtime home:** null
- **Verdict: CHANGES_REQUESTED**

## Findings

### CRITICAL
**O5-C1 — The cross-backend "0 px non-text" byte-identity regime is unachievable as specified.**
F2 (L387–389), Q10 (L433), Q11 (L434), tasks 36/43 (L758–764, L806–811), SC3 (L856) demand byte-identical non-text pixels GL/VK vs X11 with only text masked. Three independent mechanisms break this: (1) **Overlap semantics** — D3 (L59–60) claims alpha blending achieves "the same visual result," but the X11 frame demonstrably mixes GXor writes (`playingField.H:135` GXor GC; used at `shipGroup.H:237`, `bullet.H:83`) with default-GXcopy clip-masked writes (explosionGraphic gc[0..4] set XSetClipMask but no XSetFunction → GXcopy, `explosionGraphic.H:103–117`; masked rocks `rockGroup.H:184–197`). XOR accumulation over overlapping colored pixels ≠ alpha blend/top-wins for any overlap, and explosions-over-ships/rocks pervade the very sessions diffed. No XOR-emulating composition or overlap masking exists in the plan. (2) **Vector rasterization** — D2/tasks 32/35 replace Xlib-precomputed wireframe pixmaps with GPU-rasterized lines; two different rasterizers never agree pixel-exactly on diagonals, and no mask covers non-text vector regions. (3) **Thick lines/bevels** — filled quads vs XSetLineAttributes(3)/XFillPolygon (task 32, L732) differ by the plan's own admission, yet Q8 demands "0 px" face identity (L431). Consequence: tasks 36/43/48 can never pass as written → Phase-3 stall or silent gate-weakening (false pass). Respecify before Phase 3: restrict byte-identity to blitted-bitmap content, extend masks to all re-rasterized content, or specify logic-op composition.

### MAJOR
**O5-M1 — Tasks 9↔10 mutually dependent acceptance gates deadlock a literal executor.** Task 9's Change places baseline completion "(post task 10)" (L563); acceptance requires finalized hi-score/full-session baseline (L565); task 10's acceptance requires "Q9 vs the task-7/9 baseline — 0 px" (L572). Each cannot close until the other has. Trivial fix (capture hi-score baseline pre-task-10 with the valid fixture — rendering identical per the plan's own argument at L547 — or split 9 into build/finalize steps).
**O5-M2 — Phase-4 excision rule mechanically incomplete.** Rule (L442, L767) misses: task 46 "ASan clean on all three legs" + "10-min soak on GL and VK" (L831/L833); task 44 GL/VK wording (L817–819); four-plus SC rows name VK — SC1 (L854), SC3 (L856), SC4 (L857), SC9 (L862); task 48 "final three binaries" (L847); SC13 count=48 self-reference breaks when 7 rows delete (L852/L866). Dependency direction otherwise clean — enumerate the full deletion surface.
**O5-M3 — Harness determinism rests on unphased wall-clock sampling; every-frame 0-px gates will flake.** Task 9 samples "t0 + i*62.5ms boundaries" asserting wall-clock determinism under Xvfb (L563), no phase-lock. X11 endFrame performs TWO separate blits (`stage.H:258/:269`, cited by plan at L239/L598) → mid-endFrame sample captures a torn frame; usleep/scheduler jitter shifts phase; scripted XTest events can land in poll N or N+1, permanently diverging seeded trajectories. Q10/Q13 0-px-on-every-frame gates underpin ~20 acceptance rows. Mitigation: app-side frame-completion handshake (post-endFrame property/XSync barrier) or nearest-frame matching with uniqueness checks.
**O5-M4 — Task 24's X11 reorder rationale is unsound and contradicts D8.** D3 asserts blanket order-invariance (L59; encoded in SC12 L865); repo shows mixed GC functions (see C1 item 1), so GXcopy-masked draws are order-dependent relative to overlapped GXor content. Task 24 emits canonical order on all backends claiming byte-identity (L672) while D8 states "X11 keeps its existing draw sequence verbatim" (L85). Resolve: canonical order binding on GL/VK only; X11 preserves original emission order.

### MINOR
**O5-N1 — "Tasks 14-22 are file-independent once tasks 12-13 land" (L543) is false.** Every one edits shared utilities/rendering/x11Backend.H (file lists L583–L653). Parallel waves conflict; serialize backend-header edits or partition.
**O5-N2 — Hidden environment assumptions lack provisioning/verification.** (a) All five server-side X fonts must exist under Xvfb or initWindow fails hard (stage.H:134–139 null-checks all five; incl. non-standard white_shadow-48 and unusual -ibm-ergonomic-…); no font check in tasks 7/9. (b) xwd (x11-apps), ImageMagick convert/compare, libXtst/XTest assumed but absent from README prereqs. (c) llvmpipe GL 4.5 assumed for CI; task 31 fails closed (good) but no fallback documented. (d) LSan needs ptrace-friendly sandbox. (e) Task 30 vendoring has no version pinning/checksums; offline acquisition unstated; building GLFW from source requires CMake — awkward beside the no-CMake stance (L103). All fail loud rather than falsely.
**O5-N3 — Risk concentration in task 13 (L589–595):** one atomic commit re-forms 6+ ctors across 5 headers, wraps raw bodies in 9 domain headers, rewires main(), carries first mandatory-green GL/VK objects build. Red blocks 14–22 and Phases 2–3. Pre-agree a split seam (ctor changes vs body-guard wrap).

## Dimension coverage & verification notes
- **Fabrication sweep (dim 6): CLEAN — zero fabricated anchors in ~30 samples** (makefile flags, playingField/stage/options/button/score anchors exact, XtAppMainLoop/XtAppInitialize absent repo-wide, intersection2d.H exactly 998 LOC, bitmaps/ exactly 32 .xbm, _CORP_LOGO_ swap present, island list verified line-by-line, B2 Options sites verified). **Pinned census replicates exactly: independent whitelist scan returns 434 total, all 16 per-file counts match digit-for-digit.**
- **Sequencing:** dependency graph otherwise acyclic and well-ordered (26→27→34; 29→30→31; 12→14; 15→25; 16→44; barrier gates give build-green checkpoints; mid-phase failure leaves X11 tree buildable). Defects: M1 cycle, M2 excision, N1.
- **QA soundness:** seeded RNG design sound (single srand site; env-fallback preserves behavior); X11-vs-X11 baseline methodology sound modulo M3 flakiness; cross-backend regime unsound per C1.
- **USER GATE (dim 7): mechanics unambiguous — no finding.** Gate sits before any Phase-4 execution (L7, L44, L765–767); Phases 0–3+5 contain nothing requiring Vulkan to exist; post-decision handling defined by deletion rule, whose incompleteness is O5-M2.

## Overall judgment
"This is an unusually truthful artifact — every one of ~30 sampled citations verified against the repo, the 434-site census reproduces exactly, and the round-1–4 defect ledger resolves to real, checkable pointers — and its phasing, barrier gates, and ownership map show genuine execution discipline. It is not, however, execution-ready as written: the plan's central cross-backend success criterion (byte-identity on non-text regions) is mathematically unreachable given the repo's mixed GXor/GXcopy compositing and the GPU-rasterization substitution it mandates, and three sequencing/mechanics defects (the 9↔10 acceptance cycle, the incomplete Vulkan excision surface, and the unphased harness sampling) would stall or destabilize execution even on the X11-only path. All five majors are fixable with bounded edits and no scope change; none undermines the architecture. Approve after C1's identity regime is respecified and M1–M4's mechanics are corrected."

**Effort estimate:** Short (1–4h) for the plan revision — M1/M2/M4/N1/N3 targeted text edits; C1 and M3 require small design decisions before Phase 3 begins.

---
*Receipt persisted by Prometheus from the reviewer's terminal message, 2026-08-22. Full transcript: opencode session ses_fd690e861ffeRvXwLA2yyRx8ZN.*
